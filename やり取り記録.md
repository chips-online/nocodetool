# 開発やり取り記録（AI アシスタントとの対話要約）

最終更新: 2026年4月1日（作業の保存・ヘッダーUI／アコーディオンまで追記）

---

## 1. プロジェクトの目的

Studio のようなノーコードツールの**簡易版**を、まずテスト用に作る。

---

## 2. 初期の方針（コード作成前）

- 実装手順の提示を依頼された。
- **HTML と CSS は別ファイル**で、自分で編集しやすい形にするよう指定。
- その時点では**まだコードは書かない**とのことだった。

---

## 3. フロントエンド仕様（ご希望）

- **ブロック種類**: テキスト、画像、ボタン。
- **テキスト**: 色・サイズなどの装飾を変更可能に。
- **画像**・**ボタン**の追加。
- **簡単なドラッグ**で配置。
- **キャンバス**: 幅は 1200px 前後（固定）、高さは可変。

---

## 4. 実装済み機能の経緯（対話に基づく追加・変更）

| タイミング | 内容 |
|-----------|------|
| フロント初版 | `index.html` / `styles.css` / `app.js` でサイドバー、キャンバス、ブロック追加・ドラッグ・プロパティ編集。 |
| 追記 | **ダブルクリック**でテキスト／ボタンラベルをインライン編集（`contenteditable`）。編集中はドラッグしない。 |
| 追記 | **レイヤーパネル**で重なり順の一覧、「手前へ／奥へ」、選択。リスト上＝手前の説明。 |
| 追記 | サイドバー **キャンバス高さ（px）** でワークエリアの目標高さを変更。ブロック下端に合わせて自動延長。 |
| 追記 | **削除**: プロパティの削除ボタン、レイヤーの削除、Delete/Backspace（入力フォーカス時は除く）。 |
| 追記 | **ブロック JSON 仕様の固定**: `block-document-schema.js` に仕様コメントと `BLOCK_DOCUMENT_SCHEMA_VERSION`。`serializeCanvasDocument` / `applyCanvasDocument` と `window.NoCodeBlockDocument`。仕様変更は利用者の明示指示があるまで行わない方針（以降、機能追加のたびに指示に基づき拡張）。 |
| 質問回答 | 使用フレームワークは**なし**（バニラ HTML/CSS/JS）。 |
| 記録 | 上記までを **`やり取り記録.md`** に残す依頼。 |
| 追記 | **ボタン**のサイズ・余白・枠・色などをプロパティで拡張。JSON の `style` に対応フィールドを追加。 |
| 追記 | **左サイドバー固定**＋**キャンバス側のみスクロール**（`html`/`body` の高さ・オーバーフロー、`app-body` の `overflow: hidden` など）。狭い画面用メディアクエリあり。 |
| 追記 | プロパティの **スタイル系をアコーディオン**（`<details>`）で折りたたみ。削除ボタンは最下部へ。 |
| 追記 | **ボタン枠線**が効かない問題を修正（`appearance: none`、四辺の `border-*` 明示、`applyButtonBorderSides` など）。 |
| 追記 | **キャンバス**: 背景色・背景画像（URL／ファイル）・表示モード（cover / contain / repeat）。**ボタン**: 背景画像（URL／ファイル）・同モード。JSON の `canvas` / ボタン `style` を拡張。 |
| 追記 | **ボタン背景の透過**（チェックボックス＋透過 OFF 時の固体色、`backgroundColorSolid` 等を JSON に記載）。 |
| 追記 | **ブロックのセンタリング**: プロパティの X/Y の下に「水平」「垂直」「縦横」ボタンで、キャンバス `clientWidth` / `offsetHeight` 基準で中央配置。 |
| 本更新 | **ここまでのやり取りを記録として残す**依頼に応じ、上表を最新化。**センタリングは未コードだったため同タイミングで実装済み**。 |
| 2026-04-01 | **コンテナブロック**: `.canvas-block--container` と `.canvas-block__nest` で子ブロックをネスト。レイヤー・キャンバス高さ・`placeBlock` のクランプは「キャンバス直下」のみ。子の z-index は親ごと。JSON は `type: "container"` と `children` 再帰、`schemaVersion` を **2** に更新。コンテナ選択中の追加はネスト内へ。 |
| 2026-04-01 | **コンテナ**: 内側ブロックのドラッグが親にバブルしてコンテナも動く問題を修正（`onBlockMouseDown` でネスト内の別ブロックを除外、`inner !== el`）。**ネストの空きドラッグ**でコンテナ全体移動。 |
| 2026-04-01 | **コンテナサイズ**: 右下リサイズハンドル＋プロパティの幅・高さ。JSON `style.widthPx` / `heightPx`。`readContainerStyleFromNest` / `applyContainerStyleToNest` で保存・復元。 |
| 2026-04-01 | **テキスト・画像・ボタン**: キャンバス上リサイズ（右下ハンドル）。テキストは枠の width/height、画像は幅・高さ＋`object-fit: contain`（両方指定時）、ボタンは width/height。JSON にテキスト枠・画像 `heightPx`・ボタン `heightPx` を拡張。 |
| 2026-04-01 | **レイヤーパネル**: `siblingsStackSorted(parent)` で親単位の並び。**ツリー表示**（コンテナ下に子を `ul.layer-list--branch` で再帰）。「手前へ／奥へ」も同一親内のみ入れ替え。 |
| 2026-04-01 | **コピー＆ペースト**: `Ctrl/Cmd+C` / `Ctrl/Cmd+V`（テキスト入力中は無効）。クリップボード JSON（`kind: "nocodeToolBlockClip"`）＋ `internalClipboard` フォールバック。貼り付け時に ID 再採番・位置オフセット・`getAppendTargetForNewBlock` と同じ貼り付け先。 |
| 2026-04-01 | **テキスト**: 枠内の横位置 **左／中央／右**（`style.textAlign`、プロパティセレクト、JSON `style.textAlign`）。 |
| 2026-04-01 | UI: `index.html` にコンテナボタン・レイヤー説明（ツリー・キーボードショートカット）。`styles.css` にネスト・リサイズハンドル・`kbd` 等。 |
| 2026-04-01 | **ホバー時の距離表示**: ポインタがブロック上のとき、親（キャンバスまたは `.canvas-block__nest`）の四辺までの px と、重なりのある兄弟ブロックとのギャップ（マゼンタ）を線＋ラベルで表示（`app.js` の `renderHoverMeasures` 等、`styles.css` の `.canvas-measure-*`）。ドラッグ中・`contenteditable`・リサイズハンドル上では非表示。 |
| 2026-04-01 | **キャンバス背景画像の削除**: サイドバーに「背景画像を削除」ボタン（`clearCanvasBackgroundImage`）。URL／ファイル入力もクリア。 |
| 2026-04-01 | **画像ブロック**: `<img draggable="false">` でネイティブ画像ドラッグと競合しないように。キャンバス（／ネスト）へ **画像ファイルのドラッグ＆ドロップ**でブロック追加。プレースホルダ SVG の文言は **「ドラッグで画像を挿入できます」**（`defaultImagePlaceholderSrc` に統一）。サイドバー「画像」押下直後のファイルダイアログ自動表示・ブロックのダブルクリックでファイル選択する方式は、ユーザー指示により **取りやめ**（ドロップ中心に戻した）。 |
| 2026-04-01 | **画像ドロップの差し替え**: ドロップ時、**プライマリ選択**（`primarySelected()`）が画像ブロックで、かつ `selectedBlocks` に含まれる場合は **新規ブロックを作らず** そのブロックの画像をファイルで置換（`applyFileToImageBlock`）。プロパティの「画像 URL」入力に `id="prop-img-url"` を付与し、ファイル反映後に同期。`addField` の url／text 系で `numberOpts.id` により `id` を付けられるように拡張。 |
| 2026-04-01 | **お問い合わせフォーム（contact）— 縦リサイズ**: 送信ボタン直上の余白だけが伸びる問題を修正。`.canvas-block__contact-body-spacer` を廃止し、**CSS Grid**（`grid-template-rows: minmax(最小px, Nfr)`）で3項目行に余白を比率配分。`syncContactBodyGridTemplate` / `syncContactBodyGridFromFakes`。 |
| 2026-04-01 | **お問い合わせフォーム — 余白％**: 見出し〜入力エリア・項目間・最後の項目〜送信ボタン・ラベル〜グレー枠を **フォーム高さに対する％** で設定（`--contact-gap-*`、dataset、`applyContactGapsToForm`）。項目〜ボタンは `gap: max(12px, var(--contact-gap-main-button))`、既定 mainButton **3%**。 |
| 2026-04-01 | **お問い合わせフォーム — 見出しスタイル**: フォーム見出しと各項目ラベル（3つ）について **文字サイズ・色・横位置** を個別にプロパティ化。JSON: `contactTitleFontSizePx` / `contactTitleColor` / `contactTitleTextAlign`、`contactFieldLabelFontSizePx` / `contactFieldLabelColors` / `contactFieldLabelTextAligns`（各配列）。`readContactTitleStyleForPanel`、`applyContactHeadingStyles`。`.canvas-block__contact-row-label` に `display: block`（`text-align` 用）。 |
| 2026-04-01 | **お問い合わせフォーム — 枠の見た目**: 枠の**背景色** `contactFormBackgroundColor`（`readContactFormBackgroundResolved`）。枠の**角丸** `contactFormBorderRadiusPx`（既定 **8px**、新規 `chrome.form.style.borderRadius`）。CSS の `.canvas-block__contact-form` の `border-radius` は **0**（実値はインライン）。 |
| 2026-04-01 | **コンテナ**: キャンバス上の **「コンテナ」ラベル**（`.canvas-block__label`）を削除。内側 `.canvas-block__nest` の **角丸** `borderRadiusPx` をプロパティ化（JSON・`applyContainerStyleToNest`、既定 **6px**）。CSS の `border-radius` は **0**（実値はインライン）。 |
| 2026-04-01 | **`addField`**: `select` / `color` にも `numberOpts.id` で `id` を付与可能に拡張。 |
| 2026-04-01 | **作業の保存（永続化）**: リロードで消えないよう、`serializeCanvasDocument` の JSON を **`localStorage`**（キー `nocodeTool_lp_document_v2`）に保存。`setCanvasHeight()` 末尾から **約1.5秒デバウンス**で自動保存。手動は「今すぐ保存」「ブラウザから読込」「JSON をダウンロード」「JSON ファイルを読み込む」「ブラウザの保存を削除」。起動時 `tryLoadPersistedDocumentOnStartup` で復元し、成功時は `setCanvasHeight`／`renderLayers` を二重実行しない。`window.NoCodeBlockDocument` に `saveToLocalStorage` / `loadFromLocalStorage` / `downloadJsonFile` を公開。容量超過時はステータス表示。 |
| 2026-04-01 | **保存 UI の配置**: 保存まわりを**ヘッダー右上**（`.app-header__persist`）に集約。左はタイトル・説明（`.app-header__main`）。 |
| 2026-04-01 | **保存 UI の簡略化**: 常時表示は **「今すぐ保存」ボタンのみ**。説明・保存ステータス・その他の操作は **`<details>` アコーディオン**（サマリー「その他の保存・読込」、内側クラス `.persist-accordion__inner`）に格納。 |

---

## 5. 主要ファイル

| ファイル | 役割 |
|----------|------|
| `index.html` | マークアップ（キャンバス背景 UI 等） |
| `styles.css` | スタイル（レイアウト、アコーディオン、ボタン、整列ボタンなど） |
| `app.js` | 挙動一式 |
| `block-document-schema.js` | JSON ドキュメント仕様（コメント＋`BLOCK_DOCUMENT_SCHEMA_VERSION`） |
| `やり取り記録.md` | 本ファイル（対話・決定事項の要約） |

---

## 6. 本メモの目的

「これまでのあなた（AI）とのやり取りを記録として残す」という依頼に応じ、**対話の流れと決定事項の要約**を残すためのファイルです。全文のチャットログのコピーではありません。

---

## 7. 今後のメモ運用の提案

新しい依頼や仕様変更があれば、このファイルの末尾に**日付付き**で追記すると、経緯の追跡がしやすくなります。

---

## 8. 2026年4月1日時点の技術メモ（実装の参照用）

- **JSON**: `block-document-schema.js` のコメントが単一ソースの説明。`apply` は `schemaVersion` 1〜現行の範囲で読み込み。`serialize` は現行バージョンで出力。
- **コンテナ**: `finalizeContainerBlock`（ネスト空きドラッグ＋ネスト用リサイズ）、`mountBlockTree` / `serializeBlockElement` が再帰。
- **コピペ**: `copySelectedBlockToClipboard` / `pasteFromClipboard` / `mountPastedBlockEntry` / `assignFreshIdsToBlockEntry`（`app.js`）。
- **レイヤー**: `renderLayers` 内 `appendLayerBranch` が再帰ツリー化。
- **計測オーバーレイ**: 各親の `:scope > .canvas-measure-layer`（スナップガイドと別）。ホバー計測は `z-index: 9998`（スナップは 10000）。
- **画像**: ドロップは `canvas` の `dragover`/`drop`。差し替え条件はドロップハンドラ内の `primarySelected()` と `dataset.type === "image"`。
- **お問い合わせフォーム**: 枠は `.canvas-block__contact-form`、余白の％は `applyContactGapsToForm`、行グリッドは `syncContactBodyGridTemplate`、見出しは `normalizeContactHeadingStyleFromSt` / `applyContactHeadingStyles`、枠背景 `readContactFormBackgroundResolved`、枠角丸 `readContactFormBorderRadiusPx`。
- **コンテナ**: ネストは `.canvas-block__nest` のみ子。角丸・サイズ等は `readContainerStyleFromNest` / `applyContainerStyleToNest`（`borderRadiusPx` 含む）。
- **保存**: `app.js` の `LOCAL_STORAGE_DOC_KEY`、`saveDocumentToLocalStorage` / `scheduleAutoSaveToLocalStorage` / `tryLoadPersistedDocumentOnStartup`。DOM は `index.html` の `#persist-section`（ヘッダー内）、`#persist-status` はアコーディオン内。スタイルは `styles.css` の `.app-header__persist`、`.persist-accordion*` 等。
