# NoCodeTool 作業記録（〜2026-04-01）

本ファイルは、LP テンプレート調整・画像表示・新規作成ボタンなど、これまでの変更内容の記録です。

---

## 1. テキストブロック（`app.js`）

- `style.letterSpacing`（数値は `em`、文字列はそのまま）
- `style.textTransform`（`uppercase` / `lowercase` / `none`）  
  → `buildBlockFromEntry`（text）および `serializeBlockElement`（text）で反映・保存

## 2. `block-document-schema.js`

- 上記 `letterSpacing` / `textTransform` の仕様をコメントに追記

## 3. 美容サロンテンプレート（`templates.beautySalon`）

- セクション英語ラベル（FEATURE / CONCEPT / RESERVATION 等）、見出しの日英組み合わせ、キャッチ・ボタン文言の調整（英語装飾＋日本語主体）
- ボタンは日本語（例：ご予約はこちら、カウンセリングを予約）
- 日本語のタイトル・見出しは明朝体（`fontFamily: "serif"`）に統一
- CTA 長文のフォントサイズ・幅調整（1 行に収めやすく）
- ヒーロー TOP 画像：**1200×480** を維持
- FEATURE 3 枚：**352×220**、**y: 188** で横並びを統一
- CONCEPT：画像 **520×340**、**x:72**、本文 **x:640・幅 488**（728 幅レイアウトは撤回）

## 4. カフェテンプレート（`templates.cafe`）

- MENU / STORY / GALLERY / VISIT 等の英語ラベルと見出し構成
- 日本語見出しは明朝体、ボタンは日本語（メニューを見る、席の予約をする、アクセスを見る）
- MENU・GALLERY の 3 枚：**352×220**、**y: 188** で統一
- STORY：画像 **520×340**、**x:72**、本文 **x:640・幅 488**

## 5. 画像表示まわり（横並びの「幅が違って見える」対策）

- `syncImageObjectFit`：`contain` → **`cover`**（枠いっぱいに表示）
- `styles.css`：`.canvas-block__img` の **`max-width: 100%` を `none`** に変更
- 画像ブロック：ラッパー `.canvas-block` に **`width` / `height`**（px）を付与（生成時・リサイズ時）

## 6. 新規作成（白紙キャンバス）

- **`index.html`**：「今すぐ保存」隣に **「新規作成」**（`#persist-new-document`）
- **`app.js`**：`createBlankCanvasDocument()` / `startNewBlankDocument()`  
  - 確認後、ブロックなし＋`defaultTemplateCanvas()` で適用  
  - テンプレート選択を空にし、ブラウザ保存を実行  
- **`styles.css`**：`.persist-panel__primary` に `gap` 等
- **`window.NoCodeBlockDocument`**：`createBlankDocument` / `startNewBlank` を公開

---

## 変更ファイル一覧（主なもの）

| ファイル | 内容 |
|---------|------|
| `app.js` | テキスト style 拡張、テンプレート、画像 fit・ラッパー寸法、新規作成 |
| `styles.css` | 画像 `max-width`、ヘッダーボタン行レイアウト |
| `block-document-schema.js` | 仕様コメント |
| `index.html` | 新規作成ボタン |

---

## メモ

- プロジェクトは **Git 未初期化**のため、履歴は本ファイル＋手元のバックアップで管理してください。
- 初期化する場合の例：`git init` 後に初回コミットを取るとよいです。
